import styled from 'styled-components';
import {
  typography,
  space,
  layout,
  border,
  shadow,
  flexbox,
} from 'styled-system';

export const Box = styled('div')(
  typography,
  space,
  layout,
  border,
  shadow,
  flexbox
);
